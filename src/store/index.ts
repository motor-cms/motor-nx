import { createStore } from 'vuex'

export default createStore({
  state: {
    navigationItems: [],
    pageTitle: 'Default page title',
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
